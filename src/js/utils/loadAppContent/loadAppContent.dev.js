// use content JSON bundled with application - src/js/constants/content.json
// does not fetch externally hosted content JSON

const loadAppContent = () => dispatch => {};

export default loadAppContent;
